{
  "name": "ai-dev-team",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "check:worker": "node --check scripts/paired-worker.mjs",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "ci": "npm run typecheck && npm run check:worker && npm test",
    "worker": "node scripts/paired-worker.mjs",
    "worker:pair": "node scripts/paired-worker.mjs pair",
    "worker:doctor": "node scripts/paired-worker.mjs doctor",
    "worker:once": "node scripts/paired-worker.mjs once",
    "worker:start": "node scripts/paired-worker.mjs start"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
